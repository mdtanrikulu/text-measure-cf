{
  "name": "text-measure-cf",
  "version": "1.0.0",
  "description": "Cloudflare Worker MVP for generating images with Canvas WASM",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "wasm-server": "node wasm-server.js",
    "dev:full": "concurrently \"yarn wasm-server\" \"yarn dev\"",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "opentype.js": "^1.3.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "concurrently": "^9.2.1",
    "wrangler": "4"
  }
}
